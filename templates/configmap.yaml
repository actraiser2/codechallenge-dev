apiVersion: v1
kind: ConfigMap
metadata:
  name: codechallenge2-cm
data:
  SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: "{{ .Values.datasource.maximumPoolSize }}"
  SPRING_DATASOURCE_URL: {{ .Values.datasource.databaseUrl }}
  SPRING_DATASOURCE_USERNAME: {{ .Values.datasource.username }}
  SPRING_DATASOURCE_PASSWORD: {{ .Values.datasource.password }}
  SERVER_SHUTDDOWN: graceful
  SPRING_CLOUD_STREAM_BINDINGS_ACCOUNTS_IN_0_CONSUMER_AUTO_START: {{ .Values.topics.autostartTopics }}
